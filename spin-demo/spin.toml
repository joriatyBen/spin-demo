spin_manifest_version = 2

[application]
name = "spin-demo"
version = "0.1.0"
authors = ["benjamin.bajorat <benjamin.bajorat@mayflower.de>"]
description = ""

[[trigger.http]]
route = "/checkout"
component = "spin-demo"

[component.spin-demo]
environment = { DB_URL = "host=127.0.0.1 user=myuser dbname=products password=p05tgr3$" }
source = "target/wasm32-wasi/release/spin_demo.wasm"
allowed_outbound_hosts = ["postgres://localhost:5432"]
[component.spin-demo.build]
command = "cargo build --target wasm32-wasi --release"
watch = ["src/**/*.rs", "Cargo.toml"]
